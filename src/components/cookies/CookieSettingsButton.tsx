import React from 'react';

/**
 * Cookie settings re-open button.
 *
 * The user prefers a clean UI without a persistent floating cookie icon.
 * Users can still re-open preferences from the footer link in the
 * Privacy Policy page (and any "Cookie settings" link we expose elsewhere).
 *
 * This component intentionally renders nothing.
 */
export const CookieSettingsButton: React.FC = () => null;
